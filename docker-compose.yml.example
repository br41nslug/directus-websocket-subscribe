version: '3'
services:
  directus:
    container_name: directus
    image: directus/directus:9.4.3
    ports:
      - 8055:8055
    volumes:
      - ./data.db:/directus/data.db
      - ./dist:/directus/extensions/hooks/websocket-subscribe
    environment:
      KEY: '[ FILL UUID ]'
      SECRET: '[ FILL UUID ]'
      TELEMETRY: 'false'
      LOG_LEVEL: 'debug'

      DB_CLIENT: 'sqlite3'
      DB_FILENAME: './data.db'

      WEBSOCKET_PUBLIC: 'false'

      ADMIN_EMAIL: 'admin@example.com'
      ADMIN_PASSWORD: 'd1r3ctu5'

